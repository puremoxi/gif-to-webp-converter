# Changelog

## v3.6.15 — 2025-11-10
- Full export verified: includes **index.html**, complete **src/** tree, configs, validator, vendor scaffolds.
- UI: Centered header, Advanced section stacked; mutual-exclusion restored (Lossless⇄Mixed, Loop⇄Still).
- Queue cards show **resolution → FPS → frames → animation/still**.
- Picker-first saving for individual files and ZIP (fallback to classic download).
- FFmpeg loader banner with per-file progress and n/3 counter.
- Validator checks vendor/ffmpeg presence & integrity.

## v3.6.14-1
- Hotfix export (some archives previously missing src/ and index.html).

(See earlier entries in prior releases.)
